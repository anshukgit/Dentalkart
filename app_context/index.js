import {createContext} from 'react';

export const DentalkartContext = createContext();

export const DentalkartProvider = DentalkartContext.Provider;
export const DentalkartConsumer = DentalkartContext.Consumer;
